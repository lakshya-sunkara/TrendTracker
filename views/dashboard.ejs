<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>TrendTracker Dashboard</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

  
  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background-color: #0f0f0f;
      color: #fff;
      padding-top: 70px; 
    }

    .navbar {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  z-index: 999;
  display: flex;
  justify-content: flex-start; 
  align-items: center;
  padding: 1rem 2rem;
  background-color: #141414;
  box-shadow: 0 0 10px #00ffff, 0 0 20px #00ffff;
}

.navbar .brand {
  font-size: 1.7rem;
  font-weight: bold;
  color: #00ffff;
  text-shadow: 0 0 5px #00ffff, 0 0 10px #00ffff, 0 0 20px #00ffff;
  margin-right: 60rem; 
}


    .profile {
      position: relative;
      margin-right: 0rem;
      cursor: pointer;
    }

    .profile img {
      width: 42px;
      height: 40px;
      border-radius: 99%;
      border: 2px solid #00ffff;
      box-shadow: 0 0 8px #00ffff, 0 0 12px #00ffff;
      transition: transform 0.2s ease-in-out;
    }

    .profile img:hover {
      transform: scale(1.05);
    }

    .dropdown {
      position: absolute;
      top: 55px;
      right: 0;
      background: #1e1e1e;
      border: 1px solid #00ffff;
      border-radius: 12px;
      box-shadow: 0 0 10px #00ffff, 0 0 20px #00ffff;
      width: 320px;
      display: none;
      z-index: 1000;
    }

    .dropdown.show {
      display: block;
    }

    .dropdown .profile-img {
      text-align: center;
      padding: 1rem 1rem 0.5rem;
    }

    .dropdown .profile-img img {
      width: 65px;
      height: 65px;
      border-radius: 50%;
      border: 2px solid #00ffff;
    }

    .dropdown .info {
      padding: 0.5rem 1rem;
    }

    .dropdown .info p {
      margin: 5px 0;
      color: #ccc;
      font-size: 0.9rem;
    }

    .dropdown .actions {
      text-align: center;
      padding: 0.8rem;
    }

    .dropdown .actions a {
      display: block;
      padding: 0.5rem 1rem;
      margin: 0.4rem auto;
      background: #00ffff;
      color: #000;
      border-radius: 5px;
      text-decoration: none;
      font-weight: bold;
      width: 80%;
      box-shadow: 0 0 5px #00ffff, 0 0 10px #00ffff;
      transition: transform 0.2s ease;
    }

    .dropdown .actions a:hover {
      transform: scale(1.05);
    }

   
.success-message {
  position: fixed;
  top: 90px;
  right: 30px;
  background: rgba(0, 255, 204, 0.10); 
  color: #00ffe1;
  padding: 1rem 1.5rem;
  border-radius: 12px;
  font-weight: bold;
  font-size: 1rem;
  font-family: 'Poppins', sans-serif;
  text-shadow: 0 0 5px #00ffe1, 0 0 10px #00ffe1, 0 0 20px #00ffe1;
  box-shadow:
    0 0 10px rgba(0, 255, 204, 0.3),
    0 0 20px rgba(0, 255, 204, 0.5),
    0 0 30px rgba(0, 255, 204, 0.7),
    0 0 40px rgba(0, 255, 204, 0.8);
  animation: slideInRight 0.5s ease-out, fadeOutRight 0.5s ease-in 4s forwards;
  z-index: 9999;
}

.sidebar {
  width: 200px;
  background-color: #0f2535;
  position: fixed;
  top: 4%;
  left: 0;
  bottom: 0;
  padding-top: 70px;
  box-shadow: 2px 0 10px rgba(0, 255, 255, 0.1);
  z-index: 998;
  color: white;
}

.sidebar-menu {
  list-style: none;
  padding: 0;
  margin: 0;
}

.sidebar-menu li {
  padding: 15px 25px;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 15px;
  transition: 0.3s;
  font-size: 16px;
}

.sidebar-menu li:hover {
  background-color: #00ffff22;
  color: #00ffff;
}

.sidebar-menu li i {
  width: 20px;
  text-align: center;
}


@keyframes slideInRight {
  from {
    opacity: 0;
    transform: translateX(100%);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes fadeOutRight {
  to {
    opacity: 0;
    transform: translateX(100%);
    display: none;
  }
}

.upload-link {
  display: inline-block;
  margin-top: 0.6rem;
  padding: 6px 12px;
  background: #0f0f0f;
  border: 1px solid #00ffff;
  border-radius: 5px;
  color: #00ffff;
  font-weight: bold;
  text-shadow: 0 0 5px #00ffff;
  cursor: pointer;
  transition: background 0.3s ease, transform 0.2s ease;
}

.upload-link:hover {
  background: #00ffff;
  color: #000;
  transform: scale(1.05);
  box-shadow: 0 0 10px #00ffff, 0 0 20px #00ffff;
}

.neon-btn {
  display: inline-block;
  padding: 12px 24px;
  font-size: 16px;
  background: transparent;
  color: #00ffff;
  border: 2px solid #00ffff;
  border-radius: 8px;
  text-decoration: none;
  transition: 0.3s ease;
  position: relative;
}

.neon-btn:hover {
  background: #00ffff;
  color: #000;
  box-shadow: 0 0 10px #00ffff, 0 0 20px #00ffff;
}
.trending-list {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 20px;
  margin-top: 20px;
}

.trending-item {
  background: #1c2938;
  border: 1px solid #00ffff22;
  padding: 15px;
  border-radius: 10px;
  color: #fff;
  transition: 0.2s;
  box-shadow: 0 0 10px #00ffff11;
}

.trending-item img {
  width: 100%;
  border-radius: 8px;
  margin-bottom: 10px;
}

.trending-item:hover {
  transform: scale(1.02);
  box-shadow: 0 0 12px #00ffff44;
}

  </style>
</head>
<body>

  <div class="navbar">
    <div class="brand">🚀 TrendTracker</div>
    <div class="profile" onclick="toggleDropdown()">
     <img src="<%= user.profileImage && user.profileImage !== '' ? user.profileImage : '/images/default_image.png' %>" alt="Profile">


      <div class="dropdown" id="dropdownMenu">
        <div class="profile-img">
          <img src="<%= user.profileImage && user.profileImage !== '' ? user.profileImage : '/images/default_image.png' %>" alt="Profile">



          <form action="/upload-profile" method="POST" enctype="multipart/form-data" id="uploadForm">
    <label for="fileUpload" class="upload-link">➕Add Photo</label>
    <input type="file" name="profileImage" id="fileUpload" style="display: none;" onchange="document.getElementById('uploadForm').submit();" />
  </form>
        </div>
        <div class="info">
           <p><strong>User ID:</strong> <%= user.userId %></p>
          <p><strong>Name:</strong><%= user.username %></p>
          <p><strong>Email:</strong><%= user.email %></p>
         
        </div>
       <div class="actions">
  
  <button onclick="location.href='/logout'" class="neon-btn"> Logout</button>
</div>

      </div>
    </div>
  </div>
  <% if (message) { %>
    <div class="success-message">
      <%= message %>
    </div>
  <% } %>

 <div class="sidebar">
  <ul class="sidebar-menu">
    <li><i class="fa fa-home"></i> <span>Home</span></li>
    <li><i class="fa fa-search"></i> <span>Keyword Tracker</span></li>
    <li><i class="fa fa-chart-line"></i> <span>Volume Trends</span></li>
    <li><i class="fa fa-smile"></i> <span>Sentiment Analysis</span></li>
    <li><i class="fa fa-database"></i> <span>Trending Posts</span></li>
    <li><i class="fa fa-file-export"></i> <span>Reports</span></li>
  </ul>
</div>



  <div style="margin-left: 200px; padding: 2rem;">
  <h1>Welcome, <%= user.username %>!</h1>
  <p style="font-size: 18px; color: #ccc;">
    <strong>TrendTracker</strong> is your one-stop dashboard for monitoring what’s trending across platforms like <strong>Twitter</strong> and <strong>YouTube</strong>.
    As a registered user, you can:
  </p>
  <ul style="margin-top: 1rem; font-size: 16px; color: #aaa; line-height: 1.8;">
    <li>🔍 Track keywords and hashtags in real time</li>
    <li>📈 View trend volume charts and spikes</li>
    <li>😊 Analyze sentiment of tweets and video comments</li>
    <li>🎥 Explore top trending videos and viral tweets</li>
    <li>📂 Export trend reports as PDF or CSV</li>
    <li>📅 Filter trends by custom date ranges</li>
  </ul>
  <p style="margin-top: 1rem; font-size: 16px; color: #ccc;">
    Start by choosing a feature from the sidebar — whether it’s viewing trending content, analyzing sentiment, or exporting reports.
  </p>

  <h2 style="margin: 3% 0%;">Select a Keyword to Track</h2>
  <p>Click below to choose a keyword you're interested in. We’ll fetch and analyze trends across platforms like Twitter, YouTube, and Reddit.</p>

  <a href="/search" class="neon-btn">Select Keyword</a>


  <hr style="margin: 40px 0; border-color: #00ffff44;" />

<!-- 🌐 Trending YouTube Videos -->
<section>
  <h2>🌐 World Trending YouTube Videos</h2>
  <div class="trending-list">
    <% trendingYouTube.forEach(video => { %>
      <div class="trending-item">
        <img src="<%= video.thumbnail %>" alt="Thumbnail" />
        <h4><%= video.title %></h4>
        <p><%= video.channel %></p>
        <a href="<%= video.url %>" class="neon-btn" target="_blank">Watch</a>
      </div>
    <% }) %>
  </div>
</section>

<!-- 🔥 Trending Reddit Posts -->
<section style="margin-top: 40px;">
  <h2>🔥 Trending Reddit Posts</h2>
  <div class="trending-list">
    <% trendingReddit.forEach(post => { %>
      <div class="trending-item">
        <h4><%= post.title %></h4>
        <p>Subreddit: <%= post.subreddit %></p>
        <a href="<%= post.url %>" target="_blank" class="neon-btn">Read More</a>
      </div>
    <% }) %>
  </div>
</section>

<!-- 🐦 Trending Tweets -->







</div>


  <script>
    function toggleDropdown() {
      document.getElementById('dropdownMenu').classList.toggle('show');
    }

    window.onclick = function(event) {
      if (!event.target.closest('.profile')) {
        const dropdown = document.getElementById('dropdownMenu');
        if (dropdown && dropdown.classList.contains('show')) {
          dropdown.classList.remove('show');
        }
      }
    }
  </script>
</body>
</html>
